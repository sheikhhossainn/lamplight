/**
 * Theme transition overlay is now superseded by the unified UI-thread
 * `themeTransitionProgress` driver which provides 60/120fps hardware-interpolated
 * transitions without full-screen cover occlusion.
 */
export function ThemeTransitionOverlay() {
  return null;
}

